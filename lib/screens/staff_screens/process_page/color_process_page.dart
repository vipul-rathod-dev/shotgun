import 'package:flutter/material.dart';

class ColorProcessPage extends StatelessWidget {
  const ColorProcessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}