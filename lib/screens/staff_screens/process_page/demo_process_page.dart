import 'package:flutter/material.dart';

class DemoProcessPage extends StatelessWidget {
  const DemoProcessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}