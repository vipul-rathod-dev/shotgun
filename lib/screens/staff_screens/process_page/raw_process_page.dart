import 'package:flutter/material.dart';

class RawProcessPage extends StatelessWidget {
  const RawProcessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}